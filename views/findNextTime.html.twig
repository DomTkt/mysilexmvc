<h2>Formulaire</h2>

{% if stop is defined %}
    <form method="post" name="formulaire" action="{{ url('stops.seenext', {id: stop.id}) }}">

        <input type="hidden" name="id" value={{stop.id}} />

        <label for="name">Nom : </label>
        <input type="text" name="nom" value={{stop.nom}} />

{% else %}
    <form method="post" name="formulaire" action="{{ url('stops.seenext', {id: stop.id}) }}">
        <label for="name">Nom : </label>
        <input type="text" name="nom" />
        
{% endif %}
    
    <label for="name">Horaire : </label>
    <select type="text" name="horaire">
        {% for horaire in horaires %}
            <option value="h">{{horaire.heure}}</option>
        {%endfor%}
    </select>
    
    
    <label for="name">Pro Arret : </label>
    <select type="text" name="horaire">
        {% for horaire in horaires %}
            <option value="h">{{horaire.heure}}</option>
        {%endfor%}
    </select>
    <input type="text" name="nextstop" />

    <button type="submit">Chercher</button>

</form>
